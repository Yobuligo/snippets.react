/**
 * Returns true if the given {@link value} is not undefined and not null, otherwise false.
 */
export const isNotNull = (value: any): boolean => {
  return value !== undefined && value !== null;
};
