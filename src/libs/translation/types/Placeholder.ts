/**
 * This type represents a placeholder for translations.
 * A placeholder must either be a string or of type JSX.Element, to inject e.g. links.
 */
export type Placeholder = { [key: string]: string | JSX.Element };
